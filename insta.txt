Authentication==>

->register(1.Save the data of users.   2.Provide token to user)

->login
->logout(token blacklisting)
->[otp based registration]


post==>

->create,{caption:String,img_url:String,user:UserId,createdAt:Date-time}
->can see the feed,
->like posts,(collection types)
->save post


 user==>

 ->followers
 ->following


 user={
    username=String,
    email =String,
    password =String,
    bio=String,
    profile_image =String
 }